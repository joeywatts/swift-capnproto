/// Namespace for whole-runtime feature availability. This remains false until
/// the builder, framing, schema, and RPC milestones are implemented.
public enum CapnProtoRuntime {
    public static let isImplemented = false
}
